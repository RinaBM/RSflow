import { PlaceholderPage } from "@/components/ui/placeholder-page";

export function JournalPage() {
  return (
    <PlaceholderPage
      title="Journal"
      description="Search, filter and manage every trade here — coming once the Trade model ships."
    />
  );
}
